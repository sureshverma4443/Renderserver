{
  "name": "renderserver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch data.json --port 8080",
    "start": "node index.js"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
